services:
  app:
    image: ghcr.io/your_github/imported-zip-solohost-browser-native-v3zip:0.1.0
    restart: unless-stopped
    labels:
      pi.ui.primary: "true"
    ports:
      - "127.0.0.1:18195:8080"
